# TT 客户数据平台前端开发规范

版本：v1.0  
日期：2026-09-09  
适用项目：`/Users/caizi/por/web/tt_web`  
配套文档：[TT 客户数据平台开发计划](./TT客户数据平台开发计划.md)

本文作为本项目新增页面、组件、接口封装和前端评审的统一依据。技术方案已确定，本文提到的目录、组件和命令需要在工程初始化及后续开发中落实，不代表当前已经存在。

## 1. 技术栈与项目边界

### 1.1 技术选型

| 类别 | 统一方案 | 使用约定 |
| --- | --- | --- |
| 应用框架 | Vue 3 | 单文件组件，组合式 API，`<script setup lang="ts">` |
| 开发语言 | TypeScript | 开启严格检查，接口、组件和业务状态均声明类型 |
| 构建工具 | Vite | 本地开发、环境配置、生产构建 |
| PC 组件库 | Element Plus | 表格、表单、日期筛选、分页、弹窗和反馈统一使用 |
| 路由 | Vue Router | 客户页面路由、登录守卫、页面懒加载 |
| 共享状态 | Pinia | 当前用户、公司、功能权限及必要的全局偏好 |
| HTTP 请求 | Axios | 通过统一请求层调用，支持取消请求与错误处理 |
| 日期处理 | Day.js，按需启用 UTC / timezone 插件 | 账户时区和绝对时间的展示转换 |
| 样式 | CSS 变量 + scoped CSS | 统一设计变量，样式按组件归属管理 |
| 图表 | ECharts，出现图表需求时引入 | 路由或组件级按需加载 |
| 质量检查 | vue-tsc、ESLint、Prettier | 类型、代码规范和格式检查 |
| 测试 | Vitest；关键页面流程使用 Playwright | 覆盖金额/日期、请求竞态、轮询和关键业务流程 |

- 初次搭建时核实所选依赖、Node.js 的兼容关系，记录运行时版本并提交 `package-lock.json`；统一使用 npm，CI 使用 `npm ci`。
- 新增依赖应解决明确需求；不要为了可选功能提前安装多套状态、请求或 UI 方案。
- 首版按 PC 客户管理系统实现。后续若开发独立移动页面，再制定移动端组件和路由规则。

### 1.2 与现有项目的关系

参考 `admin_tp8_vue` 的 Element Plus 使用方式、页面分层、统一请求、筛选表格和自适应布局经验；在本项目使用 TypeScript。

`tt_web` 是独立客户站点，后端为 `facebook_ad_manager` 新增的客户接口。旧项目的 `/admin/*`、菜单 `jump`、迁移白名单、TP8 混合响应码和内部数据身份切换均为旧项目约定，不直接复制到客户站点。

本项目业务包含：客户登录、TT 授权、广告账户、每日消耗、每 10 分钟更新的实时查询及下载记录。平台公司管理页面按开发计划放在内部管理前端。

## 2. 目录结构与分层职责

```text
tt_web/
├── 前端开发规范.md
├── TT客户数据平台开发计划.md
├── docs/                         # 接口契约、联调与验收记录
├── public/                       # 无需构建处理的公共静态文件
├── src/
│   ├── main.ts                   # 应用初始化与全局插件
│   ├── App.vue                   # 应用根节点
│   ├── api/
│   │   ├── http.ts               # Axios 实例、响应解析、错误规范
│   │   ├── auth.ts
│   │   ├── authorizations.ts
│   │   ├── accounts.ts
│   │   ├── daily.ts
│   │   ├── realtime.ts
│   │   └── exports.ts
│   ├── types/                    # 跨模块接口与领域类型
│   ├── router/                   # 路由表、菜单配置、守卫
│   ├── stores/                   # 会话、公司、权限、全局偏好
│   ├── layout/                   # 客户站点布局
│   ├── views/
│   │   ├── login/
│   │   ├── authorizations/
│   │   ├── accounts/
│   │   ├── daily/
│   │   ├── realtime/
│   │   └── downloads/
│   ├── components/              # 跨页面公共组件
│   ├── composables/             # 跨页面复用流程
│   ├── utils/                   # 纯工具
│   ├── constants/               # 共享枚举和默认配置
│   ├── styles/                  # 设计变量、全局基础样式
│   └── assets/                  # 构建处理的图片、字体等资源
└── tests/                       # 关键业务测试与页面流程
```

单个业务页面采用：

```text
views/realtime/
├── index.vue                    # 路由参数与模块装配
├── types.ts                     # 页面内部类型
├── constants.ts                 # 默认筛选和展示配置
├── components/
│   ├── RealtimeFilters.vue
│   ├── RealtimeSummary.vue
│   ├── RealtimeTable.vue
│   └── RealtimeCampaignDrawer.vue
└── composables/
    ├── useRealtimeQuery.ts
    └── useRealtimePolling.ts
```

| 层 | 应负责 | 不应负责 |
| --- | --- | --- |
| `index.vue` | 路由读取、权限分支、模块装配和跨组件联动 | 请求底层配置、长篇字段转换、复杂轮询实现 |
| `components/` | UI 展示、局部交互，通过 props / emit 通信 | 跨页面共享状态、隐式修改其他组件内部数据 |
| `composables/` | 查询状态、分页、提交、轮询、业务流程复用 | 直接操作其他页面 DOM、无边界读写全局变量 |
| `api/` | 接口定义、参数序列化、响应适配 | 弹窗、页面路由装配、具体页面 loading |
| `stores/` | 跨页面共享且有明确生命周期的状态 | 保存所有页面列表或永久持有历史客户报表 |
| `utils/` | 无业务副作用的日期、格式、比较等工具 | 隐式发请求或依赖当前登录公司 |

视图、组件、布局和 Store 不直接创建 Axios 实例或调用裸 HTTP 请求；统一调用 `api/` 导出的业务方法。

按功能边界拆分，不为行数机械拆文件。单文件超过 400 行必须检查是否混合多个职责；超过 600 行应先拆分职责再继续新增功能，必要例外说明原因。

## 3. TypeScript、命名与组件通信

- 跨模块类型放在 `src/types`，页面私有类型放在页面目录；避免重复定义同一接口模型。
- 接口数据进入系统时使用明确类型或 `unknown` 后收窄，不以 `any` 绕过关键状态检查。
- TT 账户 ID、系列 ID、授权 ID、导出任务 ID、批次与快照标识均按不透明字符串处理；不做 `Number(id)`、`parseInt(id)` 或算术运算。
- 日期字符串和绝对时间字段在类型注释中写清含义，例如 `stats_date` 是账户自然日，`last_success_at` 是带时区的时间点。
- 金额字段采用十进制字符串；可空数据显式声明 `null`，不要用 `0` 同时表达“未采集”和“零消耗”。
- 状态使用有限的联合类型或枚举，区分授权状态、账户确认状态、采集状态和页面加载状态。
- 组件文件使用 PascalCase，例如 `AccountFilters.vue`；composable 使用 `useXxx.ts`；API 使用 `fetchXxx`、`createXxx`、`disconnectXxx` 等明确动词。
- 子组件接收 props，通过 `emit('search')`、`emit('reset')`、`emit('success')` 等事件通知父组件；禁止直接修改 props。
- `defineExpose` 仅暴露必要的 `open`、`close` 等能力，优先使用 props / emit 表达状态。
- 监听器、定时器、订阅和外部事件应在作用域结束时释放；所有可能持续进行的副作用都要有清理路径。

## 4. 页面路由与菜单

首版统一采用独立客户路由：

| 路径 | 页面 |
| --- | --- |
| `/login` | 客户登录 |
| `/portal/authorizations` | 授权中心 |
| `/portal/authorizations/result` | 授权返回结果 |
| `/portal/accounts` | 广告账户 |
| `/portal/daily` | 日账单 |
| `/portal/realtime` | 实时数据 |
| `/portal/downloads` | 下载记录 |
| `/403`、`/404` | 无权限、页面不存在 |

- 前端使用 History 模式；部署服务器需为前端路由配置 SPA 回退。`/portal/v1/*` API 与后端 OAuth 回调优先按后端规则处理，不回退到前端 HTML。
- 路由和菜单使用同一份类型化元信息，包含标题、图标和功能权限，避免两套配置漂移。
- 侧边栏在前端固定且始终展示三个入口，顺序为「授权中心 / 广告账户 / 日账单」，不请求菜单接口，也不由后端权限字段增减菜单。实时数据和下载记录不出现在菜单中；已有业务路由按需保留，下载记录通过导出结果链接进入。路由访问及具体操作继续校验权限，数据安全由后端强制保证。
- 路由守卫先恢复会话并读取 `/me`，再决定访问；仅有本地缓存不视为登录验证通过。
- 页面按路由懒加载；直接访问、菜单点击、刷新页面和浏览器前进/后退均需可用。
- URL 可保存日期、账户、分页等非敏感查询条件；所有参数先校验和规范化。URL 中不放 Token、公司切换身份或完整授权数据。
- 登录后的 `redirect` 仅接受本站允许的相对路径，阻止跳转到外部地址。

## 5. 接口与请求规范

### 5.1 统一请求层

- 业务请求统一以 `/portal/v1` 为接口前缀，地址来自环境配置；生产域名不写死在页面。
- 只创建一个客户 Axios 实例。公共层统一处理超时、响应封装、会话失效、取消请求及网络错误，业务模块负责对应接口类型与参数。
- 接口响应沿用开发计划的 `code/message/data`；列表为 `items/total/page/page_size`，金额汇总为 `summary_by_currency`，同步与快照信息为 `meta`。
- 具体业务成功码在接口契约阶段与后端冻结，在公共层集中判断；不把旧 TP8 项目的 `0/1/1001` 分支分散搬进页面。
- 服务端字段优先保持原名；确需转换时在 API 适配层统一完成，页面不重复做多种结构兜底。
- 错误转换成统一业务错误类型，保留错误码、用户可读信息和服务端请求标识；不把原始堆栈或敏感响应直接展示。
- 401 统一清理会话并跳登录，避免并发请求反复跳转或弹出多条提示；403 展示无权限，不自动伪装成空列表。
- 请求主动取消不弹错误。预期的“采集中”“授权待确认”按业务状态展示。
- 错误提示由请求层或页面中的一个明确责任方展示，避免重复 toast。

### 5.2 查询与竞态

- 页面查询维护 `loading`、`error`、`data`，在 `finally` 中恢复对应请求的状态。
- 新筛选发起时取消旧查询，或使用请求序号判断；旧响应不得覆盖新条件结果。
- 旧请求的 `finally` 不得把新请求的 loading 提前关掉。
- 列表、统计与详情的请求作用域分开；切换账户时清理旧详情、旧快照和旧分页。
- 搜索可使用防抖；提交操作防重复点击。写操作不自动重试，除非后端提供且接口契约明确使用幂等键。
- 网络请求与缓存的身份维度包含当前会话/公司；它们只用于客户端隔离状态，不作为后端权限凭据。

## 6. 公司隔离与登录状态

**后端强制限制数据归属，前端保证正确展示身份、避免缓存残留和错误复用。两部分都必须完成。**

1. 当前公司、公司类型和用户权限只从后端 `/me` 读取；客户用户不能在 UI、URL 或请求参数中自由指定 `company_id`。
2. 页头清晰展示当前公司名称；客户页面不提供本公司与其他客户公司的切换入口。
3. 首版仅公司管理员可以管理授权，管理员与只读成员都可查询和导出本公司报表；最终可用操作由后端权限字段决定。
4. 用户退出、登录失效、公司停用或重新登录为其他公司时，停止轮询、取消请求、清理 Store、报表缓存、账户选择、快照和弹窗状态。
5. 清理后旧请求即使返回，也不能重新写入新会话的数据；使用会话版本或请求作用域阻止过期响应。
6. 默认采用后端 HttpOnly Cookie 会话，落实跨域凭据及 CSRF 契约；不为图省事额外把会话凭据复制到 localStorage。
7. 浏览器不保存、展示或日志输出 TT Access Token、开发者 Secret、完整授权码等敏感内容。
8. localStorage 如用于表格偏好，应以用户和公司命名空间区分；首版不持久化账户报表、下载链接或授权详情。
9. 下载、详情和快照访问以服务端返回为准；发现 `FORBIDDEN` 或归属冲突时提示并清理相关选择，不能自行回退到内部接口。

## 7. TT 授权交互

- 点击授权后向后端获取授权 URL 和尝试 ID，前端不自行拼接 Secret 或以用户 ID 替代 OAuth state。
- 外部授权地址必须符合后端配置的允许域名和 HTTPS 规则；普通外链也需限制协议。
- 建议采用整页跳转，授权返回到结果页；若需要弹窗，验证 `postMessage` 的 origin、来源窗口及消息结构，禁止使用任意来源消息更新状态。
- 结果页区分：授权处理中、已授权且账户发现中、发现完成、待归属确认、用户取消、失败、状态已过期。
- “授权成功”不等于“报表已同步”。同步未完成时展示进度/状态入口，不显示假账户或假金额。
- 授权状态轮询使用独立的短生命周期控制；页面离开、成功、取消、失败或超过等待上限时停止，提供明确重试入口。
- 重新授权只作用于当前公司；重复回调与任务幂等由后端保证。
- 断开授权前说明停止采集的影响并确认，成功后刷新授权及账户状态，保留历史数据停止更新的提示。

## 8. 表格、筛选与页面组件

### 8.1 表格与分页

- 主表使用 Element Plus `el-table`，统一封装表格容器，采用 `sticky-head-table` 样式及本项目实现的 `useAdaptiveTableHeight`；不要直接复制旧页面固定像素偏移。
- 表格使用服务器分页、服务器排序和服务器汇总；默认每页 20 条，上限按接口约定 100 条。
- 搜索、重置、改变关键筛选条件后回到第 1 页；翻页不清空筛选。
- 日报使用中文 Element Plus 日期范围选择器，按 `start_date`、`end_date` 传递 YYYY-MM-DD 字符串，包含首尾日期，默认最近 7 天，支持常用快捷范围。当前单次最多 31 天；前后端校验起止顺序、真实日历日期及未来日期，汇总和导出沿用整个范围。
- 广告账户和日报支持按 TikTok 授权返回的用户名筛选。下拉显示用户名，提交 `authorization_id`，不使用后台登录用户名作为筛选依据；选项只来自当前公司的授权记录，断开授权的用户保留用于查历史。列表、汇总、分页及导出使用同一条件。
- `row-key` 使用稳定业务键，例如账户 ID，或账户 ID + 日期；不使用数组下标。
- 金额列右对齐并展示币种上下文，账户/系列长 ID 支持复制，长名称提供可访问的完整内容。
- 排序参数只能来自允许字段，不直接把任意 URL 内容转为后端排序语句。
- 分页中的“汇总”明确为全部筛选结果还是当前页；默认展示后端返回的全部筛选结果，不能把当前页合计标为总额。
- 空数据、未授权、待确认、同步中、失败、无权限分别展示，不用同一个“暂无数据”覆盖所有状态。

### 8.2 公共组件建议

| 组件 / composable | 职责 |
| --- | --- |
| `AccountSelect` | 当前公司账户选择，支持搜索与状态展示 |
| `ReportDateRange` | 账户日期范围输入和跨度校验 |
| `CurrencySummary` | 按币种显示后端汇总 |
| `SyncStatus` | 最近成功时间、延迟、失败或等待状态 |
| `ReportTable` | 统一表格容器、分页、加载与空状态，保留业务列插槽 |
| `ExportButton` | 发起导出并显示任务反馈 |
| `useAdaptiveTableHeight` | 根据容器可用空间计算表格高度并清理监听 |
| `useVisiblePolling` | 页面可见性、单请求轮询与生命周期清理 |

公共抽象优先解决已经出现的重复需求；避免将所有页面配置成复杂的通用 CRUD 引擎。

## 9. 实时查询与快照规范

### 9.1 采集、检查、刷新各自的含义

- 后端每 10 分钟发起 TT 采集；前端查询最近成功的当日累计快照。
- 首次进入页面立即查询本系统 API；“刷新数据”只查询本系统，不发起 TT 补采。
- 页面可见且会话有效时，默认每 60 秒检查同步状态和是否有新批次；页面隐藏、卸载或退出登录后停止。
- 使用单个定时器并在前一请求结束后安排下一次检查，避免慢请求重叠；临时网络错误退避，恢复可见时及时检查。
- 检测到新批次时提示“有新数据，点击刷新”。用户翻页或查看详情期间不静默替换快照；刷新后统一替换列表、统计与快照，并回到第 1 页。

### 9.2 快照一致性

- 首次查询取得 `snapshot_id`；分页、系列下钻和实时导出都携带同一个快照标识。
- 改变账户、日期或其他影响数据范围的条件时，丢弃旧快照并重新查询。
- `SNAPSHOT_EXPIRED` 时提示刷新，不继续拼接不同批次的列表。
- 展示每个账户的统计日期、时区和最近成功更新时间。跨账户更新时刻不一致时，展示更新时间范围、缺失账户数和陈旧账户数。
- 没有今日成功数据时显示等待；昨日数据不能显示为今日数据。
- 失败但有旧数据时保留旧值并标记失败/延迟；成功零消耗正常显示 `0`；失败或未知不转换为 `0`。
- 超过 20 分钟无成功更新建议标记“更新延迟”，阈值与后端配置保持一致；不承诺采集频率等于 TT 上游数据延迟。
- 多个批次是累计快照，绝不累计相加：10:00 为 100，10:10 为 120，当前结果必须为 120。

## 10. 每日消耗、金额与时间

- 页面使用“每日消耗”名称，与官方结算账单区分；需要充值、扣款或发票时按新增需求评估。
- `stats_date` 是账户自然日字符串，不能先转浏览器本地 `Date` 再格式化，以免日期跨天。
- 展示绝对时间时使用带 UTC 标识的后端时间戳，并明确转换到的账户时区或用户展示时区。
- 多时区账户的“今天”按各账户定义，页面给出说明；跨账户默认汇总也应展示日期语义。
- 使用账户 IANA 时区处理夏令时和半小时时区，不用固定 `+8` 或浏览器时区代替。
- 金额以十进制字符串贯穿 API 与业务状态；前端原则上不负责金额汇总。确需精确运算时使用经评估的十进制方案，不直接用浮点数加减。
- 展示小数位按币种与接口契约确定，原始数据不因 UI 格式化被覆盖；导出精度由后端统一。
- 不同币种独立显示，首版不自动换汇；图表纵轴也不能把不同币种直接混合。
- CTR、CPC、CPM 和转化指标使用后端统一口径；不平均页面上每行的比率计算总计。
- 注册/转化的归因口径未确定前，不擅自将不同字段合并或互相替换。

## 11. 表单、反馈与下载

### 11.1 表单流程

采用“校验 → 必要时确认 → API → 成功反馈 → finally 恢复状态”的顺序。

- 登录、搜索、刷新、普通导出不增加无意义的二次确认。
- 断开授权等有明确业务影响的操作必须说明影响并确认；用户取消后不调用 API。
- 提交期间按钮禁用，避免重复提交；失败保留可重试输入，成功后按需要刷新父列表。
- 字段错误靠近字段展示，全局失败显示明确原因；禁止捕获异常后静默当作成功。

### 11.2 导出与下载

- 导出由后端异步生成，前端提交当前筛选条件和实时快照标识；不通过“只导出当前页”冒充完整导出。
- 下载记录展示等待、生成中、成功、失败和已过期；轮询同样遵守可见性和生命周期规则。
- 后端负责导出创建、生成和下载时的权限校验；前端不拼接公开文件目录或猜测文件名。
- 使用受保护下载接口。读取 Blob 前核验响应类型，避免将登录失效的 JSON 当作 Excel 保存。
- 临时对象 URL 使用完成后释放；公司或用户会话变化后清理下载状态与链接。
- 导出成功提示文件范围与状态；文件内容保留币种、账户时区、统计日期与数据更新时间。

## 12. 视觉、交互与可访问性

- 在 `styles/tokens.css` 统一维护主色、文字、背景、边框、状态色、间距、圆角与阴影；使用 Element Plus 主题变量统一组件风格。
- 页面采用稳定顺序：标题与说明 → 筛选 → 汇总/状态 → 表格 → 分页。核心按钮位置保持一致。
- 间距优先使用 4/8/12/16/24/32px 设计刻度；避免同类页面各自定义完全不同的字号和间距。
- CSS 优先 scoped；全局样式只处理主题、布局基础与统一组件规范，深层覆盖应限制在业务容器内。
- 适配常见桌面宽度，窄屏筛选可换行、表格可横向滚动，不依靠固定整页宽度避免布局问题。
- 标签、表单错误、按钮焦点和键盘操作保持可用；图标按钮提供文字说明，状态不只用颜色表达。
- 对没有值的数据统一使用 `—`，对真实零使用 `0`；金额正负与账户状态按统一视觉规则显示。
- 客户页面不展示内部投手、线路、数据库字段、原始错误堆栈等不帮助客户决策的实现信息。
- 内容默认按文本渲染；确需富文本时经过允许列表清洗后使用，禁止直接渲染不可信 `v-html`。

## 13. 性能、配置与工程检查

- 路由和较重图表按需加载；用构建结果决定分包策略，避免预先复制旧项目所有打包规则。
- 表格请求限制日期跨度和分页大小，详情按打开时加载；不在浏览器一次获取全部客户历史数据。
- 前端环境变量只保存公开配置，如 API 基础地址；所有 `VITE_*` 值都应视为会进入浏览器，不能存放密钥。
- 接口契约与 Mock 同步维护，Mock 覆盖成功、空值、延迟、失败、权限拒绝和长 ID；生产构建不能启用测试账户或虚假数据。
- 测试使用虚构数据，不把客户 Token、Cookie、真实完整账单或生产凭据提交仓库。

工程初始化时提供以下脚本；当前仅为要求，不表示脚手架已生成：

| 命令 | 要求 |
| --- | --- |
| `npm run dev` | 启动本地开发 |
| `npm run typecheck` | `vue-tsc --noEmit`，检查 Vue 与 TypeScript 类型 |
| `npm run lint` | 使用 ESLint 检查代码，并限制视图层直接依赖 Axios |
| `npm run format:check` | 使用 Prettier 检查格式 |
| `npm run test` | 使用 Vitest 执行必要的单元/组件测试 |
| `npm run test:e2e` | 使用 Playwright 执行已配置的关键页面流程 |
| `npm run build` | 执行类型检查后进行 Vite 生产构建 |
| `npm run preview` | 本地检查构建产物 |

构建成功不等同接口可用或权限隔离通过；上线验收按开发计划执行。

## 14. 开发完成与评审清单

每次开发先确定页面、组件、composable、API 和后端契约的影响范围。改动后完成对应验证，必要时同步本文与开发计划；不将本项目路由或技术规则套用到其他仓库。

- [ ] 代码符合 TypeScript 与分层约定，无页面直接 HTTP 请求、隐式跨组件状态操作。
- [ ] 路由、菜单及权限一致，直接访问与刷新可用。
- [ ] 接口参数和响应类型与契约一致，长 ID、空值、日期、币种处理正确。
- [ ] 查询/重置回第 1 页，旧请求不能覆盖新结果，loading 和错误状态正确收尾。
- [ ] 公司身份取自后端；退出或换账号后没有旧报表、下载、弹窗和请求残留。
- [ ] 实时轮询能停止、不会重叠；快照分页/下钻/导出一致，不重复累计批次。
- [ ] 成功、空数据、真实零、待确认、同步中、延迟、失败、无权限均有明确展示。
- [ ] 授权、解绑和下载流程符合权限及敏感信息处理要求。
- [ ] 运行与改动相关的类型、lint、构建和测试；未运行或失败项如实说明。
- [ ] 权限、授权、快照和金额等关键改动有针对性测试或联调证据。
- [ ] 联调覆盖客户 A/B 及本公司：篡改 ID 无法越权，前端隐藏入口不作为唯一验证。

文档和纯样式的小改动按实际影响检查，不为低影响变化增加镜像实现的测试；身份、数据查询与报表口径的改动必须验证真实边界。

## 15. 参考与约定归属

- 本项目范围、公司隔离、接口规划与排期：[TT 客户数据平台开发计划](./TT客户数据平台开发计划.md)。
- 本规范负责前端技术与工程约定；接口字段和业务含义以共同冻结的接口契约为准，发现不一致先修正文档与契约再实现。
- 参考来源：[admin_tp8_vue 前端系统规范](/Users/caizi/por/qianyi/admin_tp8_vue/docs/frontend-system-spec.md)、[现有依赖配置](/Users/caizi/por/qianyi/admin_tp8_vue/package.json)。
- 本规范取代开发计划早期的 Arco 选型，客户前端统一使用 Element Plus。

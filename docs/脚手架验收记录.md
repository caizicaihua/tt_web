# 脚手架与登录页验收记录

> 此文件保留初始脚手架验收历史。当前已接入开发环境 Mock，最新结果见文末与 [Mock 接入说明](./Mock接入说明.md)。

日期：2026-09-09  
产品名称：广告数据后台  
开发预览：`http://127.0.0.1:5175/login`

## 本次交付

- Vue 3 + TypeScript + Vite + Element Plus + Pinia + Axios 脚手架与依赖锁文件。
- 登录页、自定义品牌标识、桌面与窄屏布局、表单校验、密码显隐、管理员帮助。
- Cookie 会话接口封装、公司身份校验、权限路由、菜单与后台基础布局。
- 授权、账户、日报、实时、下载的模块入口。模块当前明确显示待开发状态。

## 已执行检查

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm run lint` | 通过 |
| `npm run format:check` | 通过 |
| `npm run test` | 12 项通过：站内跳转限制、异常目标、长 ID、公司身份完整性 |
| `npm run build` | 通过，输出 `dist/` |
| npm 安装审计 | 安装时报告 0 个已知漏洞，结果仅代表该时点 |
| 桌面 1440 × 960 | 登录页内容完整，表单与示意图排版正常 |
| 手机 390 × 844 | 单列表单完整，无横向溢出，核心按钮可见 |
| 空表单提交 | 账号与密码分别显示字段错误 |
| 后端不可用 | 展示连接失败提示，保留输入，恢复提交按钮，不进入工作台 |
| 密码显隐 | 按钮带可访问名称，切换后输入 type 为 text；默认 password |
| 忘记密码 | 正常打开与关闭管理员帮助弹窗 |
| 模拟登录成功 | 成功响应后再读取 `/me`，显示测试公司并进入授权中心 |
| 模拟导航与退出 | 可切换实时模块；退出后返回登录页 |
| 模拟无权限用户 | 直接访问账户页进入 403，无业务页面数据展示 |

登录成功、公司身份、退出及无权限流程使用 Playwright CLI 的浏览器网络拦截和虚构响应验证；没有创建真实账号、没有修改后端，也没有把 Mock 接入产品代码。检查结束后已移除全部模拟规则并恢复正常登录页。

## 范围限制

真实登录依赖后端 `/auth/login`、`/me`、`/auth/logout` 及 Cookie/CSRF 契约完成联调。OAuth 授权、报表数据、采集和真正的公司数据隔离仍需按开发计划实施；前端菜单与路由验证不能代替后端权限验收。

截图位于本地 `output/playwright/login-desktop.png`、`login-mobile.png`、`workspace-scaffold.png`。这些检查产物已被 Git 忽略。

## 后续变更：开发环境 Mock

2026-09-09 已按需求加入本地 Mock，原记录中“没有内置 Mock/演示账号”和业务占位页说明仅对应初始脚手架。当前状态见 [Mock 接入说明](./Mock接入说明.md)；真实后端和 TikTok 授权仍待联调。

- 类型检查、ESLint、Prettier、23 项 Vitest 测试和生产构建全部通过。
- 测试覆盖三家公司隔离、只读成员权限、会话过期/退出、错误密码、过滤与分页、实时快照固定、断开授权停止更新、授权 state 归属/过期/重复使用、导出下载归属。
- Playwright 使用实际本机 Mock 服务验证登录、刷新恢复会话、实时数据列表、创建导出任务、CSV 文件下载，浏览器控制台 0 错误、0 警告。未使用浏览器网络拦截替代接口。
- 已检查桌面实时数据页面和手机登录页；截图为 `output/playwright/mock-realtime-desktop.png` 与 `mock-login-mobile.png`。
- 显式设置 `VITE_ENABLE_MOCK=true` 进行生产构建仍关闭 Mock；最终 `dist` 扫描未包含模拟账号、密码或服务端模拟公司数据。

## 后续变更：授权用户名与日期范围

- 账户、日报及共用的实时查询增加授权用户名筛选，接口使用公司范围内的 `authorization_id`；列表显示用户名，汇总、分页、CSV 导出沿用条件。
- 日报改为中文 Element Plus 日期范围选择器，默认最近 7 天，提供今天、昨天、最近 7/30 天、本月快捷范围。接口使用 `start_date`、`end_date`，包含首尾日期，最多 31 天。
- 类型检查、ESLint、Prettier、34 项单元测试和构建通过；新增同名跨公司隔离、授权用户与日期组合筛选、跨月/时区边界、首尾日期、非法范围、超过 100 条的完整导出测试。
- 浏览器实际选择 `ads_growth` + 最近 7 天，得到 7 条记录；再选择“昨天”，仍保留授权筛选，得到 1 条记录。生产产物扫描未发现模拟授权用户名。

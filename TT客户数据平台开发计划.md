# TT 客户数据平台开发计划（前后端合并版）

版本：v1.0  
编写日期：2026-09-09  
前端项目：`/Users/caizi/por/web/tt_web`  
后端项目：`/Users/caizi/por/facebook_ad_manager`  
文档状态：开发方案与工作量估算，尚未实施。

## 1. 目标与首版范围

其他公司通过客户页面，使用自己的 TikTok for Business 账号给我方开发者应用授权，查询其广告账户信息、每日广告消耗及每 10 分钟更新的实时数据，并导出报表。

核心要求：**本公司账户、外部公司 A 的账户、外部公司 B 的账户，在授权归属、数据存储、查询、采集队列、缓存、导出和运营操作中均可明确区分。**

### 1.1 已确定的需求

- 前端在 `tt_web` 开发，后端复用 `facebook_ad_manager` 的 TT 接口接入能力。
- 提供广告账户信息查询、广告日数据查询，以及 TT 实时数据查询。
- 实时采集沿用每 10 分钟一次的频率；这是用户提供的现行业务配置。
- 前后端开发统一安排，重点保证外部客户与本公司数据隔离。

### 1.2 本计划采用的首版假设

- “日账单”暂指按广告账户时区统计的每日广告消耗；页面正式名称建议为“每日消耗”。官方结算账单、充值、扣款、发票另行立项。
- 客户需要在页面查看和导出数据；供客户自有系统调用的开放 API 暂不包含。
- 首版支持账户级汇总、广告系列明细；广告级实时明细列为后续增强项，现有广告级采集代码可作为基础。
- 公司及客户登录账号由平台管理员开通；首版不做公开注册、套餐收费和复杂组织架构。
- 首版一个客户登录账号只隶属一家公司；同一公司可以有多个登录账号和多个 TT 授权。
- 同一广告账户在平台内只归属一家公司；跨公司共管不在首版范围内，冲突需由平台管理员核实处理。
- 首次授权后补拉最近 7 个已结束的账户自然日；更长历史范围由管理员按需发起，受实际接口可查范围约束。

以上为排期基线，阶段 P0 确认后冻结；范围变化需同步调整接口与工期。

## 2. 现有代码基础与改造依据

以下结论来自代码检查，未连接生产数据库核实任务运行频率、实际权限、账户规模及数据完整性。

| 能力 | 现有位置（相对后端仓库） | 复用方式与注意点 |
| --- | --- | --- |
| TT API 封装 | `app/common/sdk/tiktok/TiktokBase.php`、`TiktokAdvertiser.php`、`TiktokCampaign.php` | 复用请求、账户信息、报表及分页能力；补充客户需要的账户级报告封装 |
| 授权回调 | `plugin/saiadmin/app/controller/CallbackController.php::tiktokCallback` | 已有授权码换 Token；当前直接把 `state` 当管理员 ID，需补一次性状态校验及公司绑定 |
| 授权保存 | `app/queue/redis/tiktok/authorized/AuthorizedTiktokAccountConsumer.php` | 当前按 `core_user_id` 更新授权且关联内部投手；客户授权采用独立模型 |
| 账户同步 | `app/queue/redis/tiktok/account/TiktokAccountListInfoConsumer.php` | 当前主表按 `advertiser_id` 更新，并调用内部投手映射；客户链路使用独立落库服务 |
| 调度入口 | `app/utils/RedisQueueUtil.php::getTiktokDataRedisQueue` | 当前遍历有效授权账户，批次按 10 分钟划分；上线前核实数据库中定时任务配置 |
| 实时系列数据 | `app/process/tiktok/TiktokCampaignRealtime.php` | 拉取当日累计指标并写入 ClickHouse；可抽出纯采集逻辑，避免带入内部线路和投手处理 |
| 实时广告数据 | `app/process/tiktok/TiktokAdRealtime.php` | 为后续广告级明细保留复用入口 |
| 每日数据 | `app/process/tiktok/TiktokCampaignDayPivotTask.php`、`app/queue/redis/tiktok/campaign/TiktokCampaignDayConsumer.php` | 已有跨日调度、系列日报及写库；客户链路补齐账户日报、状态和历史回补 |
| 查询与导出 | `plugin/saiadmin/app/controller/facebook/AdExpensesController.php`、对应 `AdExpensesLogic.php` | TT 对账、实时查询及导出缺少统一的公司范围约束，不直接作为客户接口开放 |
| 现有前端 | `plugin/ad_webman_vue/src/views/facebook/adAccount/`、`insights/reconciliation/`、`insights/realtime/` | 参考字段与交互，客户页面独立开发 |

特别注意：现有账户同步还会向 `tiktok_webhook_auth_subscription` 队列传递授权信息，注释显示由 `fb_mate` 消费。外部客户首版不自动进入这条内部订阅链路；如确实需要 Webhook，必须先核实下游是否支持公司隔离。

## 3. 总体架构与公司隔离方案

### 3.1 首版架构决策

采用“本公司现有业务表 + 外部客户独立业务表”的方式。外部客户之间在每张业务表中使用非空 `company_id` 隔离。可部署在现有数据库服务中，不要求首版增加服务器。

```text
客户前端 tt_web
    ↓ 客户登录会话
客户 API /portal/v1/*
    ↓ 公司上下文 + 角色校验 + 账户归属校验
客户授权 / 账户 / 每日消耗 / 实时查询 / 导出服务
    ├─ 关系数据库：公司、用户、授权、账户、日报、同步与导出任务
    ├─ ClickHouse：客户实时账户及系列快照
    └─ Redis：客户采集队列、锁、缓存、限流
                ↓
          共用 TT SDK → TT API

本公司原有后台 → 原有业务服务与数据表
双方通过统一账户归属登记表检查账户所属公司
```

不直接复制原采集类后原样运行：其中包含投手查找、线路匹配、内部产品归因等逻辑。抽取无业务副作用的 API 读取与字段转换方法，分别交给内部、客户写库服务使用。

### 3.2 公司身份与账户归属规则

1. 创建唯一的本公司记录，`company_type=internal`；每家客户创建独立记录，`company_type=external`。本公司 ID 由配置读取，不在业务代码中硬编码为固定数字。
2. `company_id` 由后端根据登录用户生成，客户接口禁止用户通过参数指定或切换公司。
3. `company_type` 区分本公司与外部公司，`company_id` 区分具体哪一家公司；不能只加一个“是否外部户”字段。
4. 广告账户建立全局归属登记，唯一键为 `(platform, advertiser_id)`；公司 ID 是服务归属的唯一来源，不从账户名称、TT 返回的公司名称、`ts_id` 或 `team_id` 推断。
5. 本公司存量账户先核对清单再登记。无法确认的记录进入 `pending_review`，不自动对客户开放，也不强行划入本公司。
6. 客户授权后取得的账户列表可能包含多家公司账户。只有归属已核实为当前公司的账户才允许启用同步；未登记账户进入待确认列表，由平台管理员核实后激活。
7. 已归属其他公司的账户触发 `ACCOUNT_OWNERSHIP_CONFLICT`，不覆盖原归属、不更新对方 Token、不展示对方公司名称或历史数据。数据库唯一约束与事务防止并发抢占。
8. 同一公司重复授权采用幂等更新；一个账户可关联同公司多个有效授权，选定一个采集授权，防止重复调度。
9. 跨公司迁移账户首版走人工流程：停采集、核实归属、失效缓存与任务、指定生效时间、重新授权。历史数据保留原公司归属，不随最新账户关系自动转移。
10. 停用公司或解除授权后，立即停止新增采集；已排队任务在执行及发布结果前再次检查有效性。历史数据保留并标记停止更新；公司停用后客户登录和下载权限同时失效。

### 3.3 需要落实隔离的位置

| 层面 | 后端规则 | 前端表现 / 验收方式 |
| --- | --- | --- |
| 登录 | 独立客户会话与身份校验，绑定用户、公司、角色 | 客户仅看到自己的公司名称 |
| 授权 | 一次性 `state` 绑定用户、公司、开发者应用及到期时间 | 展示授权成功、同步中、待确认、失效等状态 |
| 列表与详情 | 所有查询强制公司范围；账户与系列 ID 二次校验 | 修改 URL 或请求 ID 也不能访问别家公司 |
| 统计与筛选项 | 总额、分页总数、账户下拉、批次列表都使用相同公司范围 | 不泄露其他公司数量、名称或消耗 |
| 写库 | 客户写库仅使用客户表；内部写库仅使用内部表，并验证登记归属 | 内部后台总额不因新增客户而上涨 |
| 队列 | 内外队列分开；客户任务携带公司、账户、授权记录 ID | 客户任务堆积不占满内部任务并发 |
| 缓存与锁 | 含客户数据的 Key 包含公司、账户和相关查询条件 | A、B 相同日期查询不会互相命中 |
| 导出 | 创建任务、生成文件、下载时均校验公司与用户权限 | 复制另一客户的下载地址无法下载 |
| 内部业务 | 外部户不参与本公司投手、线路、产品利润及自动调控任务 | 内部现有流程回归验证 |
| 平台管理 | 单独的平台管理权限；查询外部数据需明确选定公司并记录访问 | 普通内部员工不自动拥有全部客户数据权限 |

平台级 API 速率桶可以按开发者应用共享，用于控制 TT 总配额；其中不保存客户报表数据。

## 4. 数据模型与存量接入计划

下表为逻辑表设计。后端在 P0/P1 核实实际数据库版本、索引和 ClickHouse 表引擎后提交具体 DDL。

| 表 / 模型 | 关键字段与约束 | 用途 |
| --- | --- | --- |
| `tt_companies` | `id`、`name`、`company_type`、`status` | 本公司及客户公司档案 |
| `tt_portal_users` | `id`、`company_id`、登录标识、密码摘要、角色、状态 | 客户身份；首版公司管理员和只读成员 |
| `tt_account_registry` | `platform`、`advertiser_id` 全局唯一；`company_id`、确认状态、归属生效时间 | 阻止跨公司重复归属及误入内部链路 |
| `tt_portal_authorizations` | `company_id`、`developer_app_id`、`core_user_id`、加密 Token、权限、状态、授权时间 | 唯一约束 `(company_id, developer_app_id, core_user_id)` |
| `tt_portal_authorization_accounts` | `company_id`、`authorization_id`、`advertiser_id`、关系状态 | 记录同公司多个授权与账户关系，支持撤权和切换有效授权 |
| `tt_portal_accounts` | `company_id`、`advertiser_id`、名称、BC ID、币种、IANA 时区、状态、选定授权 ID | 客户账户资料；关联全局归属登记 |
| `tt_portal_account_day` | `company_id`、`advertiser_id`、`stats_date`、币种、日指标、同步状态、版本时间 | 账户日汇总，唯一键为公司 + 账户 + 日期 |
| `tt_portal_campaign_day` | 上述字段加 `campaign_id`、系列名称 | 系列日明细，唯一键包含系列 ID |
| `tt_portal_realtime_account` | 公司、账户、账户日期、批次、尝试版本、指标、采集时间 | ClickHouse 中的账户累计快照 |
| `tt_portal_realtime_campaign` | 上述字段加系列 ID | ClickHouse 中的系列累计快照 |
| `tt_portal_sync_runs` | 公司、账户、日期、数据类型、批次、状态、页数、行数、起止时间、脱敏错误 | 采集任务及成功发布记录，包括成功零行 |
| `tt_portal_export_jobs` | 公司、用户、报表类型、筛选条件、快照版本、状态、文件位置、有效期 | 可追溯且有权限保护的导出 |
| `tt_portal_audit_logs` | 操作者、公司、操作、目标、时间、结果 | 记录归属确认、授权、停用、迁移及跨公司管理操作 |

实施约束：

- TT 广告账户、系列等长 ID 在 API 与前端始终使用字符串，避免 JavaScript 精度损失。
- 金额存储使用 Decimal；API 返回金额字符串，不用浮点累加账单金额。金额精度按接口实际值保留，展示位数与导出规则统一。
- 公司类型从公司档案读取，避免多个表分别维护类型导致不一致。
- 所有客户业务记录必须具备有效公司 ID；缺失公司上下文时拒绝处理，不默认查全部。
- 客户授权通过开发者应用配置引用密钥，不在每条客户记录中重复复制应用 Secret；Token 加密存储，密钥从服务器配置或密钥服务加载。
- 不返回或记录完整 Token、Secret、授权码。现有回调记录完整换 Token 响应的行为需修正。
- 客户数据中的 `developer_app_id` 专门表示 TT 开发者应用，避免与现有内部产品 `app_id` 混用。
- 客户门户只读客户表；即使账户 ID 相同，也不能通过内部表补出历史数据。

### 4.1 本公司存量接入顺序

1. 只读盘点现有 TT 账户、授权、同步任务和外部订阅消费者，导出待核对的归属清单。
2. 确认本公司账户后写入归属登记；含糊或重复归属的账户隔离处理。
3. 给原内部授权、账户同步、定时调度和写库入口增加归属校验，防止旧授权看到外部账户后自动写入内部主表。
4. 内部新增账户同样先确认归属，不能因为走了旧回调就自动视为本公司户。
5. 未完成归属改造与存量核验前，外部公司保持关闭状态；不先让外部数据进入原表再补隔离。
6. 不迁移原始内部日报和实时数据到客户表；确认属于外部公司的历史记录，如需导入，单独制定清单和审计流程。

## 5. 授权、同步与实时数据口径

### 5.1 客户授权流程

1. 客户登录后发起授权，后端根据当前公司生成一次性随机 `state`，建议有效期 10 分钟。
2. 页面跳转 TT 授权页面；授权回调域名和重定向地址使用后端配置的允许列表。
3. 后端核验 `state` 的有效期、用户、公司及应用，并进行幂等处理；重复回调不重复创建任务。
4. 后端换取并保存 Token，创建账户发现任务，回到前端授权结果页。
5. 前端查询授权任务状态，明确区分“已授权”和“账户同步已完成”。
6. 对已确认归属的账户启动资料同步、首轮实时采集和历史日报补拉；待确认和冲突账户不采集报表。
7. 取消授权、Token 无效、权限不足、没有可用账户、网络失败均返回可理解的状态和重试方式。

前端只访问本系统接口。回调结果不得通过 URL 携带 TT Token。若采用弹窗返回，只向配置的前端源发送结果标识，并验证消息来源；同时提供普通页面跳转路径。

### 5.2 每 10 分钟实时采集

**“实时”定义：每 10 分钟发起一次采集，查询最近成功批次的当日累计数据；并非每次点击页面都直接请求 TT。**

- 按 UTC 时间计算统一采集槽位，例如 `floor(timestamp / 600) × 600`；另行保存账户 IANA 时区和账户自然日。
- 每个有效外部账户每个槽位最多创建一个任务组；队列载荷包含 `company_id`、`advertiser_id`、`authorization_id`、`batch_id`、任务类型，不传完整 Token。
- 消费时重新读取有效授权并核验归属；最终发布数据前再校验公司、授权和账户有效性。
- 首版采集账户级报告和系列级报告，SDK 与权限支持在 P0 用真实样本验证；需要完整处理分页。
- 每轮采集使用独立尝试版本；全部必需分页成功、落库完成后，才把该账户该日期的快照标记为可见。
- 失败或部分写入的版本不参与前端查询，查询只选择已经发布的版本；重试幂等，不依赖 ClickHouse 后台合并完成来避免重复计数。
- 账户级与系列级数据使用同一任务组标识；只有完整成功的任务组才发布，避免页面汇总与明细分别取到不同批次。
- 成功返回零行必须记录“同步成功、无消耗”；网络失败不得当作零消耗。
- 调度加随机错峰、有限并发、应用总限流及公司限额；单家公司失败不阻塞其他公司。
- 失败采用有限次数的退避重试；过时任务不与新批次无限堆积，重试不得改变原账户日期或把旧批次发布成最新批次。
- 独立客户队列保留本公司采集并发。共享 TT 应用配额时为内部采集预留容量，对历史补拉和导出设置较低优先级。

### 5.3 实时查询与页面刷新

| 项目 | 约定 |
| --- | --- |
| 默认日期 | 各账户自己的“今天”；多时区账户必须展示统计日期和时区 |
| 默认数据 | 每个账户在目标账户日期下最近一次完整成功的快照 |
| 跨账户汇总 | 可以使用各账户最近成功快照，但返回最早/最晚更新时间、陈旧账户数和缺失账户数，提示更新时间不一致 |
| 无今日数据 | 展示“等待今日数据”；昨日数据不得冒充今日数据 |
| 数据新鲜度 | 返回 `last_success_at`、`next_scheduled_at`、`data_age_seconds`、`sync_status`；采集频率不等同 TT 上游数据保证延迟 |
| 陈旧提示 | 建议超过 20 分钟无成功更新提示“更新延迟”；当前失败且有旧数据时保留旧数据并展示失败状态 |
| 页面刷新 | 刷新按钮只重查本系统数据；页面可见时建议每 60 秒检查新批次，隐藏时暂停，避免重复轮询 |
| 手动采集 | 首版仅平台管理员可以发起受限的补采；客户页面刷新不创建 TT 拉取任务 |
| 分页一致性 | 首次查询生成绑定公司与筛选条件的 `snapshot_id`，后续分页、汇总与导出固定对应账户批次集合 |
| 快照过期 | 返回明确的 `SNAPSHOT_EXPIRED` 状态，前端提示刷新；导出任务创建时固化版本引用，不因页面快照过期而改用另一批次 |

重复快照不能相加。例如同一账户 10:00 累计消耗 100，10:10 累计消耗 120，最新消耗是 **120**，不能显示为 220。首版不提供“这 10 分钟消耗”指标，避免把平台数据修正简单当作增量消费。

### 5.4 每日消耗口径

- 账户级日报优先读取账户维度报告；系列明细单独读取。不要在未验证覆盖范围的情况下，把系列求和当作 TT 官方账户总额。
- 账户级与系列级核对相同币种、账户时区、日期、指标定义和查询范围；样本差异要查明原因并记录，不能静默修平。
- 日数据以 TT 返回的账户日期为准，使用 IANA 时区处理夏令时与半小时时区，不仅保存固定 UTC 偏移量。
- 账户跨日后安排昨日初次采集，建议次日较晚时段复查，并滚动回补最近 3 个已结束自然日；调度时间可配置。
- 日报成功零行、失败、补拉中、已更新分别记录，展示最近成功更新时间；“当日结束”不等于平台数据永不修正。
- 历史补拉按账户与日期拆分，使用低优先级队列，与实时队列分开。
- 不同币种分别汇总；首版不做隐式汇率换算。
- CTR 按总点击/总展示计算，CPC 按总消耗/总点击计算，CPM 按总消耗/总展示 × 1000 计算，分母为零时返回约定空值。
- 覆盖人数、频次等不可简单相加的指标不做跨账户总计；首版核心展示消耗、展示、点击，转化类指标在 P0 固定归因口径后再纳入。
- 导出与页面使用同一权限范围、筛选和统计口径，包含币种、账户时区及数据更新时间。

## 6. 前端页面与后端接口一起设计

前端确定使用 Vue 3 + Vite + TypeScript + Vue Router + Pinia + Element Plus，参考 `admin_tp8_vue` 的组件使用方式和分层架构。具体目录、路由、接口封装、状态管理及验收要求见 [前端开发规范](./前端开发规范.md)。接口路径是新设计的约定，不代表现有项目已经存在这些接口。

| 页面 / 模块 | 前端工作 | 配套后端工作 | 完成标准 |
| --- | --- | --- | --- |
| 登录与布局 | 登录、退出、会话失效、公司名称、菜单及无权限页 | 客户身份、公司状态、角色、登录限流；客户端会话与内部后台隔离 | 外部用户无法用客户会话访问原管理接口 |
| 授权中心 | 授权按钮、回调结果、等待状态、重新授权、解绑 | 安全授权发起、回调、授权状态、账户发现、归属校验 | 授权完成后能看到成功账户及待确认/失败数量 |
| 广告账户 | ID/名称/BC/状态及授权用户名筛选，币种、时区、更新时间，进入报表 | 账户分页、详情、同步状态、授权用户选项；所有筛选项按公司过滤 | A 公司只得到 A 的账户、授权用户与数量 |
| 每日消耗 | 日期、账户及授权用户名筛选，按币种汇总，账户日报、展开系列、导出 | 账户与系列日报接口、汇总、补拉状态、导出任务；统一传递授权筛选条件 | 页面与导出在同口径下金额一致 |
| 实时数据 | 今日消耗卡片、账户列表、系列明细、最近更新时间、延迟提示、手动刷新 | 10 分钟任务组、发布快照、固定快照查询、更新状态 | 不累加多批次，不把失败显示为零 |
| 下载记录 | 导出任务状态、失败重试提示、下载、过期展示 | 异步导出、受保护文件、有效期和下载权限校验 | 无法下载其他公司文件 |
| 平台管理 | 在现有内部后台增加公司列表、开通账号、归属确认、同步状态入口 | 公司开停用、客户角色、账户确认、补采、审计 | 必须明确选公司，普通内部账号无法管理客户 |

前端横向要求：统一加载/空数据/失败状态，筛选与 URL 同步，切换账户时取消旧请求，长 ID 保持字符串，日期不受浏览器本地时区隐式转换影响。注销时清理客户数据缓存，轮询在卸载组件或切换公司会话后停止。

### 6.1 客户 API 清单

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| POST | `/portal/v1/auth/login`、`/portal/v1/auth/logout` | 登录与退出 |
| GET | `/portal/v1/me` | 当前用户、公司、角色及功能权限 |
| POST | `/portal/v1/tiktok/authorizations/start` | 创建授权尝试，返回允许跳转的授权 URL 与尝试 ID |
| GET | `/portal/v1/tiktok/oauth/callback` | TT 服务端回调入口，验证 state 后返回前端 |
| GET | `/portal/v1/tiktok/authorizations` | 本公司授权列表 |
| GET | `/portal/v1/tiktok/authorization-attempts/{id}` | 某次授权及账户发现进度 |
| POST | `/portal/v1/tiktok/authorizations/{id}/disconnect` | 停用本系统授权关系与采集；是否调用上游撤权在 P0 核实支持及影响范围 |
| GET | `/portal/v1/tiktok/accounts`、`/portal/v1/tiktok/accounts/{id}` | 账户列表与详情 |
| GET | `/portal/v1/tiktok/reports/daily/accounts` | 账户日报、分页、按币种汇总 |
| GET | `/portal/v1/tiktok/reports/daily/campaigns` | 指定账户的系列日报 |
| GET | `/portal/v1/tiktok/reports/realtime/accounts` | 最新成功账户快照及 snapshot_id |
| GET | `/portal/v1/tiktok/reports/realtime/campaigns` | 固定快照下的系列数据 |
| GET | `/portal/v1/tiktok/sync-status` | 当前公司或指定账户的同步进度与新批次信息 |
| POST / GET | `/portal/v1/exports` | 创建导出 / 查询本公司可访问的导出记录 |
| GET | `/portal/v1/exports/{id}/download` | 校验当前权限后下载 |

平台管理接口使用独立前缀 `/platform/v1/tt-companies/*`，不与客户路由共享“管理员跳过公司过滤”的逻辑。

接口共同约定：

- 保持统一响应封装 `code/message/data`；列表包含 `items`、`total`、`page`、`page_size`，金额汇总放在 `summary_by_currency`。
- 报表元信息放在 `meta`，包括统计日期语义、时区信息、更新时间范围、缺失/陈旧数量以及快照标识。
- 日期参数使用 `YYYY-MM-DD`，时间戳使用带 UTC 标识的 ISO 8601；同时返回账户 IANA 时区。
- 查询参数使用统一的 `advertiser_id`、`campaign_id`、`start_date`、`end_date`，避免新接口沿用多个别名。
- 授权完成后保存 TikTok 返回的用户名，授权列表返回 `username`，账户及日报返回 `authorization_id`、`authorization_username`。前端展示用户名但以 `authorization_id` 过滤；后端先校验公司和授权归属，再查询关联账户，避免重名/改名产生歧义。新增 `GET /portal/v1/tiktok/authorization-users` 返回当前公司可查询的授权用户选项（含已断开授权的历史记录），查询成员无需授权管理权限也可使用。重复授权保持记录关联，同一广告账户经多个授权可见时需去重，不能重复统计消耗；具体关联表与真实字段在后端联调时确认。
- 默认每页 20 条，上限 100 条；日报单次日期范围建议最多 31 天，超大范围通过导出或管理员补拉处理。
- 客户请求不接受公司范围字段；SQL 使用参数绑定，排序字段采用允许列表。
- 分别定义 `AUTH_REQUIRED`、`FORBIDDEN`、`AUTHORIZATION_INVALID`、`ACCOUNT_OWNERSHIP_CONFLICT`、`SYNC_PENDING`、`SYNC_FAILED`、`SNAPSHOT_EXPIRED`、`EXPORT_EXPIRED`。同步中可作为正常业务状态返回，不统一包装成服务器异常。
- P1 交付接口定义文件或等价的可共享接口文档及样例响应，前端依据相同契约制作 Mock，后续联调避免重复改字段。

## 7. 前后端合并开发排期

估算前提：1 名前端 + 1 名熟悉现有项目的后端，需求按首版基线，具备可用开发者应用与测试账户。角色可同时开展工作，但接口、隔离和联调存在先后依赖。

**首版按 15–20 个工作日安排，即约 3–4 周；前端约 9–12 人日，后端约 13–17 人日，包含各自联调与缺陷修复。** 平台审核等待、额外账户开通及新范围不计入编码工期。

| 阶段 | 建议时间 | 前端任务 | 后端任务 | 共同交付 / 退出条件 |
| --- | --- | --- | --- | --- |
| P0 范围和数据核对 | 第 1–2 日 | 页面结构、字段清单、授权与异常流程草图 | 核实 TT 应用权限、10 分钟任务配置、样本账户、内外归属、存量队列与 Webhook；验证账户级报告 | 范围基线、数据口径、测试账户清单、容量估算；可以开始接口设计 |
| P1 隔离与接口基础 | 第 3–5 日 | 初始化工程、布局、登录、请求封装、路由守卫、Mock | 公司与客户身份、归属登记、建表、索引、权限中间件、接口契约、内部入口归属校验 | 使用本公司/A/B 测试数据验证基础隔离；客户数据尚不进入正式同步 |
| P2 授权与账户闭环 | 第 6–8 日 | 授权中心、结果页、账户列表与详情、平台管理基础页 | 授权状态校验、Token 管理、账户发现、归属确认、重复授权、断开与停用、首轮采集触发 | A/B 可分别授权；冲突账户不覆盖，账户列表不会串公司 |
| P3 每日消耗与导出 | 第 9–11 日 | 日报筛选、账户/系列明细、币种汇总、导出与下载记录 | 日报采集、跨日调度、历史补拉、查询、异步导出、下载鉴权 | 样本日报与 TT 同口径核对；导出与页面一致 |
| P4 实时查询 | 第 12–14 日 | 实时页、快照分页、更新与延迟提示、轮询、系列下钻 | 每 10 分钟独立调度、幂等采集、完整批次发布、最近成功快照、失败重试、限流 | 连续多批次可查询，120 不会被累加成 220，失败有明确状态 |
| P5 验证与试运行 | 第 15–17 日 | 真实数据联调、权限/时区/空状态检查、页面问题修复 | 跨公司越权测试、容量与队列验证、内部业务回归、告警及回滚演练 | 选一家客户试运行，验证跨日和持续实时同步；满足上线验收 |
| 缓冲 | 第 18–20 日 | 联调余项与体验修正 | 外部接口差异、数据口径差异和试运行问题处理 | 达到验收后再扩大客户范围 |

前端 P3/P4 的静态页面可在接口等待期间推进；后端优先完成身份和公司隔离，随后打通真实数据。前端页面完成不代表具备对外开放条件。

## 8. 开发拆分与代码落点

### 8.1 前端建议结构

```text
tt_web/
  src/
    api/           # auth、authorizations、accounts、daily、realtime、exports
    types/         # 与接口契约对应的数据类型
    router/        # 客户路由及登录状态守卫
    stores/        # 当前用户、公司与会话状态
    views/
      login/
      authorizations/
      accounts/
      daily/
      realtime/
      downloads/
    components/    # 日期与账户筛选、币种汇总、同步状态、表格空状态
    composables/   # 页面可见性轮询、分页与快照查询
    utils/         # ID、日期和金额展示工具
  docs/            # 后续接口契约、联调记录与验收记录
```

平台管理页面放在现有内部前端 `plugin/ad_webman_vue`，使用专门的平台管理权限；客户站点不包含内部管理菜单。

### 8.2 后端建议模块

在现有 Webman 项目中新增 `portal` 模块或等价命名空间，明确 Controller → Service → Repository / SDK 的职责：

- `PortalAuth`、`CompanyContext`、`CompanyAccountAccess`：客户身份、公司上下文、账户归属与角色检查。
- `PortalTiktokAuthorizationService`：授权尝试、回调与 Token 生命周期。
- `PortalTiktokAccountService`、`AccountOwnershipService`：资料同步、账户发现、归属确认和冲突处理。
- `TiktokReportReader`：从现有 SDK 和采集代码抽取的纯报告读取组件，不查内部投手或线路。
- `PortalTiktokDailyService`、`PortalTiktokRealtimeService`：客户报表采集与业务口径。
- `PortalTiktokReportRepository`：固定公司范围的查询、快照选择、分页与汇总。
- `PortalSyncRunService`、`PortalExportService`：批次发布、任务状态、异步导出与文件权限。
- 独立队列：`tt_portal_authorization`、`tt_portal_accounts`、`tt_portal_realtime`、`tt_portal_daily`、`tt_portal_backfill`、`tt_portal_exports`。
- 复用纯 SDK 时补充内部回归测试；原内部采集入口增加归属检查，但不切换成客户表或客户队列。

## 9. 关键验证与验收标准

### 9.1 公司隔离必须通过

测试准备：本公司 I、外部公司 A、外部公司 B，各有独立账户；再准备一个重复授权账户和一个归属待确认账户。

- [ ] A 的列表、详情、账户下拉、搜索、总数、汇总、同步状态、日报、实时、导出都只包含 A 的数据。
- [ ] A 手动改成 B 或本公司账户 ID、系列 ID、授权 ID、任务 ID、快照 ID、导出 ID 时无法访问，也不泄露名称与数量。
- [ ] A 传入 `company_id=B` 或伪造平台管理员字段无法扩大范围。
- [ ] A、B 同时授权同一账户时，只有已确认的归属有效，冲突不覆盖原数据与 Token。
- [ ] A 的账户即使出现在本公司某个旧 Token 的可访问列表中，也不会写入内部表、触发内部投手映射或进入内部下游订阅。
- [ ] 普通内部员工不能默认查询全部客户；平台管理员必须明确选公司，操作有记录。
- [ ] 客户停用或断开授权后，已排队任务、运行中任务发布、缓存与下载均按最新权限处理。
- [ ] 本公司现有报表、队列及原授权流程通过回归，新增客户不会改变本公司数据总量与归属。

### 9.2 实时与日报必须通过

- [ ] 连续至少 3 个 10 分钟槽位完成采集和发布；记录调度、开始、完成的实际耗时。
- [ ] 同账户重复投递、超时重试、部分分页失败、部分写库失败都不会产生重复汇总或发布半批数据。
- [ ] 10:00 累计 100、10:10 累计 120，页面及导出最新结果为 120。
- [ ] 失败保留最近有效数据并标记异常；成功无消耗和未成功采集可以区分。
- [ ] 页面跨页、查看系列、导出使用同一快照集合；新批次到达不会让当前分页重复或漏行。
- [ ] 多账户最新批次不同，页面能显示最早/最晚更新时间与缺失、陈旧数量。
- [ ] 跨账户自然日、半小时时区、夏令时样本及浏览器时区变化不改变账单日期口径。
- [ ] 不同币种不直接相加；长 ID 不丢精度；金额、CTR、CPC、CPM 与接口约定一致。
- [ ] 日报样本与 TT 后台/报告在相同日期、时区、币种和归因口径下核对，差异记录可解释。
- [ ] 试运行包含一次真实跨日同步；历史补拉不会拖慢实时任务。

### 9.3 必要的技术检查

- 后端对公司范围、归属冲突、OAuth 状态、批次发布、汇总及导出权限编写自动化测试；用数据库/队列集成测试覆盖真正的权限边界。
- 前端完成类型检查、生产构建，以及登录→授权→账户→日报/实时→导出的关键流程验证；状态和日期工具按风险补充测试。
- P0 确定首批账户数及增长预期。按“账户数 × 每轮报告类型与分页请求数 ÷ 600 秒”估算请求速率，额外计入账户信息、重试及补拉，并核实实际 TT 配额。
- 在约定规模下，实时队列可持续消化，无持续增长的积压；记录查询 P95、采集完成耗时和 TT 限流情况，目标值在 P0 根据基线冻结。
- 检查授权响应、日志、错误提示、浏览器存储和导出文件，不包含 Secret 或完整 TT Token。
- 若使用 Cookie 会话，落实 HttpOnly、Secure、SameSite 及状态变更请求的 CSRF 校验；跨域来源限定实际部署域名。

## 10. 发布、观察与回滚

1. 部署公司与客户模块、独立表和队列，但保持客户功能开关关闭；保存原内部调度配置及数据库变更记录。
2. 完成本公司账户归属核对及旧同步入口保护，验证原业务正常。
3. 在测试环境接入 A/B 测试账户完成隔离测试，使用独立队列与回调地址。
4. 生产先为一家明确授权的客户开启，观察 1–2 天，至少覆盖一次跨日与多个实时批次。
5. 观察客户/本公司队列积压、TT 应用总限流、最近成功采集年龄、日报补拉进度、查询异常和导出失败率。
6. 通过验收后按公司逐步开通；新增大客户前复核采集容量和配额。
7. 回滚以关闭客户开关、停止客户队列并禁用客户入口为主，不删除已确认的归属信息或历史数据；保留审计记录。
8. 不通过关闭公司校验来恢复服务。内部归属保护继续保留，避免旧任务把客户账户重新吸入内部表。
9. 如发现跨公司访问或数据混入，立即暂停受影响客户入口、采集和下载，保留现场后按账户清单修正；验证通过才恢复。

## 11. P0 需确认但不妨碍准备工作的事项

| 事项 | 计划默认值 / 核实内容 | 影响 |
| --- | --- | --- |
| 日账单含义 | 每日广告消耗；如需官方结算账单需另评估 | 接口范围和工期 |
| 实时维度 | 账户 + 系列；广告明细后续 | 采集请求量、表结构和页面数量 |
| 首批规模 | 客户公司数、账户数、平均系列数及未来增长 | 并发、配额、存储和试运行规模 |
| 历史范围 | 首次补拉 7 日；更长范围管理员按需发起 | 补拉队列和数据可用时间 |
| 数据保留期 | 实时原始快照建议先按 30 日、日报长期保留评估容量；具体期限上线前确认，未确认不启用自动删除 | 存储成本及历史查询 |
| 指标定义 | 核心消耗/展示/点击；转化与注册口径由业务确认 | 与 TT 后台对账是否一致 |
| TT 应用状态 | 当前应用的外部广告主授权、报表权限、回调配置及限流配额 | 是否能实际接入客户；不能只凭内部已可用推断 |
| 下游服务 | `fb_mate` Webhook 消费者是否涉及客户场景 | 是否需要额外项目改造 |
| 账号与域名 | 公司账号由平台开通；确定客户域名、API 域名、回调及部署环境 | 登录、跨域、授权联调 |

## 12. 完成交付物

- `tt_web` 客户前端源代码、配置示例和部署说明。
- `facebook_ad_manager` 客户模块、必要的内部归属保护、数据库变更及队列配置。
- 接口契约与样例、公司/账户归属规则、日报和实时数据口径说明。
- 本公司存量归属核对结果及未确认记录清单。
- 公司隔离、报表一致性、实时幂等、导出权限和内部回归的验收记录。
- 首家公司试运行记录、监控项、开关操作和回滚说明。

参考：现有仓库代码，以及 [TikTok API for Business 授权文档](https://business-api.tiktok.com/portal/docs?id=1738373164380162)。具体外部授权能力和报告字段以开发阶段实际应用权限及官方接口验证为准。
